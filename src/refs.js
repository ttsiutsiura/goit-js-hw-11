const formEl = document.querySelector('#search-form');
const searchButtonEl = document.querySelector('button');
const galleryEl = document.querySelector('.gallery');
const loadMoreButtonEl = document.querySelector('.load-more');
const startImgEl = document.querySelector('.start-img');

export { formEl, searchButtonEl, galleryEl, loadMoreButtonEl, startImgEl };
